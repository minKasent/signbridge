/**
 * Module analytics — thống kê sử dụng: lắng nghe sự kiện GlossRecognized từ module
 * translation (không gọi trực tiếp), lưu bảng gloss_usages và trả số liệu gộp
 * qua GET /api/analytics/summary cho dashboard /stats.
 */
package vn.signbridge.analytics;
