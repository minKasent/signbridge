package vn.signbridge.identity;

/**
 * ADMIN — quản trị từ điển, duyệt dữ liệu.
 * CONTRIBUTOR — người góp mẫu ký hiệu (bạn bè tham gia thu dataset).
 * USER — người dùng thường (dùng phiên dịch).
 */
public enum Role {
	ADMIN, CONTRIBUTOR, USER
}
