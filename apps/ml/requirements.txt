# Serving (chạy local trên máy dev / máy demo)
fastapi>=0.115
uvicorn[standard]>=0.30
numpy>=2.0
onnxruntime>=1.28

# Huấn luyện dùng chung venv này khi chạy local (Colab tự có torch):
# torch (bản CPU: pip install torch --index-url https://download.pytorch.org/whl/cpu)
# onnxscript (cần cho torch.onnx.export)
